###This is the directory for Assignment 8 
###To write a Python script that reads data from an input file, counts the total number of lines it contains, extracts the first two lines, and writes those extracted lines into a brand-new output file — all using safe, standard Python file- handling techniques.
