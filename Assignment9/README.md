### This is the directory for Assignment 9
### To write a Python program that reads structured data from a CSV file, converts that data into JSON format, and writes the resulting JSON into a new output file — using Python's built-in csv and json modules
